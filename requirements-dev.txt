# Development and test dependencies
# These are NOT installed in the production Docker image.
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=8.3
pytest-cov>=5.0
httpx>=0.27          # Required by FastAPI TestClient

# Code quality
ruff>=0.5.0
black>=24.0
pre-commit>=3.7

# Jupyter / EDA (local exploration only)
ipykernel
matplotlib>=3.8
seaborn>=0.13
notebook
statsmodels

# Model comparison (Phase 2 — not deployed to production)
lightgbm>=4.6.0
